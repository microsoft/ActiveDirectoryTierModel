# beast — History

## Current: Windows LAPS Audit Bugfix (T013) — 2026-07-16

**✅ T013 BUG FIXES (Wolverine lab-confirmed defects):**

**Bug A (SELF false-positive):** `Test-TierModelWinLapsAcl` SELF detection replaced. The original `Find-LapsADExtendedRights | ExtendedRightHolders -contains 'NT AUTHORITY\SELF'` never returns true (LAPS doesn't surface SELF in ExtendedRightHolders). Fix: pre-compute `$lapsSchemaGUIDs` from schema NC before the delegation loop, then detect SELF via `Get-Acl "AD:$resolvedOuDn"` filtering for non-inherited ACEs whose IdentityReference = 'NT AUTHORITY\SELF' and ObjectType ∈ lapsSchemaGUIDs. Read/Reset detection via `Find-LapsADExtendedRights` is unchanged. Mirrors planner logic (Get-TierModelWinLapsAcl) exactly.

**Bug B (StrictMode .Type on Decryptor findings):** `Audit-TierModel.ps1` consolidated reporting had 3 `$_.Type` accesses in `Where-Object` on `$result.Findings`. WinLaps Decryptor findings use `.Status` (not `.Type`), so StrictMode -Version Latest threw PropertyNotFoundException. Fixed all 3: error-findings filters now check `($_.PSObject.Properties.Name -contains 'Type' -and $_.Type -eq 'Error') -or ($_.PSObject.Properties.Name -contains 'Status' -and $_.Status -eq 'Error')`; drift-findings filter guards with `($_.PSObject.Properties.Name -contains 'Type') -and $_.Type -eq 'Drift'`.

Parse OK, 101/101 Pester, Enable-TierModelAuditing.ps1 intact.

**Essential Patterns (for future work):**
1. **SELF ACE detection:** `Find-LapsADExtendedRights` NEVER surfaces NT AUTHORITY\SELF in ExtendedRightHolders. Always use `Get-Acl "AD:$ouDn"` + non-inherited filter + LAPS GUID filter. Pre-compute GUIDs from schema NC once before the loop — never inside it.
2. **Mixed Findings shapes:** When multiple audit cmdlets with different Findings schemas feed the same consolidated reporter, always guard `.Type` / `.Status` etc. with `$_.PSObject.Properties.Name -contains 'Type'` before comparing. StrictMode -Version Latest throws on any missing property access, even in Where-Object scriptblocks.
3. **Audit result shape:** { TotalChecked, Compliant, Missing, Mismatched, Errors, Drift, Findings, DurationMs, CorrelationId }. For Decryptor, Findings use {GpoName, Expected, Actual, Status} (not the ACL shape). Drift = Missing+Mismatched+Errors for decryptor (errors = GPO not found / group not found = also drift).
4. **FullDeployment wrapper shape:** EntityType + Summary@{TotalAcls, Compliant, Missing, Mismatched, Errors, Drift} + Findings + DurationMs + CorrelationId. TotalAcls = TotalChecked for Optional Feature cmdlets.
5. **Consolidated reporting switch:** Both the `totalChecked` switch (before summary) and `entityChecked` switch (per-entity breakdown) must include new EntityTypes; adding `'WinLaps ACL', 'WinLaps Decryptor'` to the `$_ -in` set for TotalAcls handling.
4. **Dynamic standalone header:** Build `$standaloneLabelStr` from active Include flags at top of standalone block; reuse at results header. Keeps "MSA/gMSA/dMSA" hardcoding out of WinLaps-only runs.
5. **Decryptor audit pattern:** Get-GPO -All | Where-Object DisplayName -like $pattern → must match exactly 1 → Get-GPRegistryValue for ADPasswordEncryptionPrincipal → compare -ieq expected "NETBIOS\sAMAccountName". Missing = not set or empty; Mismatched = set but wrong; Compliant = matches.
6. **Opt-in enforcement:** WinLaps audit blocks are gated by `if ($IncludeWinLaps)` inside `if ($activeIncludeCount -gt 0)`. $includeParameters includes $IncludeWinLaps so it increments activeIncludeCount correctly. Plain -FullDeployment without -IncludeWinLaps never enters WinLaps blocks.

## Learnings — 2026-07-16

- **T013 Audit Integration:** Test-TierModelWinLapsDecryptor is NEW (not a modification of Test-TierModelWinLapsAcl). Both cmdlets have the same flat output shape but different Findings sub-shapes: ACL uses {Type, ResourceType, Identifier, Property, ExpectedValue, ActualValue, Details}; Decryptor uses {GpoName, Expected, Actual, Status}. Different shapes are fine — the FullDeployment wrapper only uses Summary.* for consolidated totals; per-finding detail display in the per-entity breakdown relies on Findings[].Type='Drift' which neither WinLaps cmdlet uses, so sub-item detail is correctly absent in FD mode (same as MSA/gMSA/dMSA).
- **Module version:** Stayed at 1.2.0 (same unreleased WinLaps feature). Test-TierModelWinLapsDecryptor added to FunctionsToExport without a version bump per Joel's direction.
- **Decryptor Drift formula:** Drift = Missing + Mismatched + Errors. Errors (GPO not found, ambiguous match, group resolution failure) count as drift because they represent unverifiable state that could be masking real drift.

## Previous: Windows LAPS Decryptor — Lab-Proven (2026-07-15)

**✅ Wolverine FD Validation:** 681 applied (643 baseline, +38 delta), all 6 non-DC GPO ADPasswordEncryptionPrincipal entries validated, idempotent converged. Ready for Joel UAT.

**Implementation:** ConfigureLapsDecryptor Phase 10 via Set-GPRegistryValue HKLM\Software\Microsoft\Windows\CurrentVersion\Policies\LAPS→ADPasswordEncryptionPrincipal. Module v1.2.0, 101 Pester pass.

**Essential Patterns (for future work):**
1. **StrictMode scoping:** Shared variables resolve BEFORE conditionals, not inside branches.
2. **ResourceType in display:** Branch on `$_.ResourceType -eq 'LapsPermission'` to render lapsOperation/allowedPrincipals.
3. **ACE IsInherited (PS7):** Use `Get-Acl "AD:<dn>"`, not nTSecurityDescriptor; filter `IsInherited=$false`.
4. **Group lookup:** Use `Get-ADGroup -Filter "Name -eq '<display name>'"`.
5. **FD plan regeneration:** Regenerate at EXECUTION time when plans bake resolved principals.
6. **ADPasswordEncryptionPrincipal:** `Set-GPRegistryValue -Key 'HKLM\...\Policies\LAPS' -ValueName 'ADPasswordEncryptionPrincipal' -Value 'NETBIOS\sAMAccountName'`.
- 2026-07-15T14:15:00+08:00 — **ALL 6 LAPS GPO DECRYPTORS SET + LAB RESET.** After Joel confirmed T0 PAWs works end-to-end, set ADPasswordEncryptionPrincipal on the remaining 5 GPOs: T0 Servers→Tier0ServerOperators, T1 PAWs→Tier1Admins, T1 Servers→Tier1ServerOperators, T2 PAWs→Tier2Admins, T2 EUD→Tier2DeviceOperators. All 6 verified via Get-GPRegistryValue. DC GPO confirmed untouched (DSRM = Domain Admins). Then reset lab to `WinLapsSchema` checkpoint (clean schema-extended state, no tier model deployed). VM is AD-ready for upcoming deploy-script code work. **ConfigureGPO integration note:** GPO Phase 3 "Configure GPO security templates" handles GptTmpl.inf (URA/Restricted Groups) via `Update-TierModelGPOConfig` → `New-TierModelGptTmplContent` → `Resolve-TierModelPrincipalSid` (name→SID at deploy time). The LAPS decryptor is a REGISTRY policy (Set-GPRegistryValue → Registry.pol), NOT a security template (GptTmpl.inf), so it does NOT belong in GPO Phase 3. Natural home: Phase 10 (-IncludeWinLaps), after the GPO has been created/imported/linked by the GPO phase. Group resolution uses the same `Get-ADGroup -Filter "Name -eq '<name>'"` → `"$netbios\$sam"` pattern already in the WinLaps planner.
- 2026-07-15T13:56:00+08:00 — **LAB PROVEN: Set-GPRegistryValue recipe for ADPasswordEncryptionPrincipal.** Programmatically set the LAPS authorized-decryptor on GPO `*- Tier 0 PAWs Windows LAPS - Computer` in the TierLab. **Exact recipe:** `Set-GPRegistryValue -Name '<GPO DisplayName>' -Key 'HKLM\Software\Microsoft\Windows\CurrentVersion\Policies\LAPS' -ValueName 'ADPasswordEncryptionPrincipal' -Type String -Value 'DOMAIN\sAMAccountName'`. **Value format:** ADMX confirms REG_SZ, maxLength=128. MS docs (LAPS.adml + Learn) accept three formats: `DOMAIN\group`, `group@domain.fqdn`, or SID string. Recommended: `DOMAIN\sAMAccountName` (human-readable in GP reports, consistent with AllowedPrincipals format). SID is the fallback for cross-domain. **Group resolution:** `Get-ADGroup -Filter "Name -eq 'Tier 0 Admins'" -Properties SID` → `TIERLAB\Tier0Admins` (SID: `S-1-5-21-3172251048-2974515427-512640283-3102`). Build value as `"$((Get-ADDomain).NetBIOSName)\$($group.SamAccountName)"`. **Verification:** `Get-GPRegistryValue -Name '<GPO>' -Key 'HKLM\Software\Microsoft\...\LAPS'` confirmed `ADPasswordEncryptionPrincipal = TIERLAB\Tier0Admins [String]` alongside unchanged `ADPasswordEncryptionEnabled = 1 [DWord]`. **GPO→group mapping for deploy-script integration:** Tier 0 PAWs→Tier0Admins, Tier 0 Servers→Tier0ServerOperators, Tier 1 PAWs→Tier1Admins, Tier 1 Servers→Tier1ServerOperators, Tier 2 PAWs→Tier2Admins, Tier 2 EUD→single wrapper group (Joel dropped Help-desk; Tier2DeviceOperators only), DCs→none (DSRM always Domain Admins). **Rotation reminder:** existing passwords stay encrypted to old principal until rotated. MS Learn citations: https://learn.microsoft.com/en-us/windows-server/identity/laps/laps-management-policy-settings, https://learn.microsoft.com/en-us/windows-server/identity/laps/laps-concepts-overview.
- 2026-07-15T13:18:00+08:00 — LAPS decryptor = ADPasswordEncryptionPrincipal GPO setting defaults to Domain Admins; read ACL != decrypt; DSRM ignores it; single-principal constraint; re-rotation needed. **DIAGNOSIS:** Joel's lab symptom (delegated groups can reset but cannot view/decrypt LAPS passwords) is caused by all 7 LAPS GPOs having `ADPasswordEncryptionEnabled=1` but NO `ADPasswordEncryptionPrincipal` set. Per Microsoft Learn, this defaults the CNG-DPAPI encryption to Domain Admins. The AD read ACL (`Set-LapsADReadPasswordPermission`) grants permission to retrieve the encrypted blob, but decryption requires being (or being a member of) the authorized decryptor principal. Lab confirmed: `Get-LapsADPassword` on both TierLab-PAW and Tierlab-Server shows `AuthorizedDecryptor = TIERLAB\Domain Admins`. **FIX:** Set `ADPasswordEncryptionPrincipal` per GPO to the tier-appropriate group (e.g., `TIERLAB\Tier0Admins` for PAWs, `TIERLAB\Tier0ServerOperators` for Member Servers). After setting, force password rotation so new blob encrypts to the new principal. **CONSTRAINTS:** Single-principal only (LAPS doesn't support multi-recipient encryption); DSRM ignores this setting (always Domain Admins); EUD dual-read-group needs a wrapper group. **CONFIG CHANGE:** add `decryptorGroup` field to `tiermodel-winlaps.json`; defaults to `readGroup` when omitted. GPO registry key: `HKLM\Software\Microsoft\Windows\CurrentVersion\Policies\LAPS` valueName `ADPasswordEncryptionPrincipal` type REG_SZ. Full recommendation in `.squad/decisions/inbox/beast-winlaps-decryptor-diagnosis.md`.
- 2026-07-15T11:42:00+08:00 — WinLaps OU/group dependency reporting moved from prereq gate (Test-TierModelPrerequisites Gates 4+5) to planner (Get-TierModelWinLapsAcl) for GPO-consistent "Dependency Errors:" formatting. Schema (Gate 1), LAPS module (Gate 2), and DFL (Gate 3) remain the sole WinLaps prereq hard-stops. Planner now emits "Target OU '<name>' does not exist - create OUs first" and "Required group '<name>' does not exist - create Groups first" matching Get-TierModelGpo pattern. MSA/gMSA/dMSA untouched — they already report via planner. Schema-missing message reworded to Joel-approved friendly text (no code prefix, no x/6 count).
- 2026-07-14T13:33:00+08:00 — BUG 5b FIX: **ACL read method for IsInherited reliability**. In PS7, `Get-ADOrganizationalUnit -Properties nTSecurityDescriptor` can misreport ACE `IsInherited=True` for ACEs that were explicitly/directly set (e.g. LAPS SELF ACEs set by Set-LapsADComputerSelfPermission). `Get-Acl -Path "AD:<dn>"` returns the SAME ACEs with the CORRECT `IsInherited=False`. **PATTERN**: always use `Get-Acl "AD:<dn>"` (AD: PSDrive, auto-created on ActiveDirectory module import) when detecting directly-set ACEs via IsInherited; do NOT rely on nTSecurityDescriptor property for IsInherited checks. If strict multi-DC targeting is needed, use `[ADSI]"LDAP://$DC/$dn"` + `.ObjectSecurity` instead.
- 2026-07-14T12:53:00+08:00 — BUG 5 FIX: **SELF detection false positive from inherited ACEs**. Every AD OU INHERITS ≥2 NT AUTHORITY\SELF ACEs from the domain root (dnsHostName, SPN, Personal-Information self-write). The original detection (`$selfAces.Count -ge 2` on ALL SELF ACEs) was always true → SELF never applied on first deploy. **CORRECT detection**: LAPS SELF ACEs are NON-INHERITED (`IsInherited=$false`, directly set by Set-LapsADComputerSelfPermission), while default SELF ACEs are inherited. Filter: `IdentityReference='NT AUTHORITY\SELF' AND IsInherited=$false AND ObjectType ∈ ms-LAPS schemaIDGUIDs`. Count ≥1 means already applied. Fresh OU = 0 non-inherited SELF ACEs = SELF planned+applied. 2nd run = non-inherited LAPS ACEs present = SELF skipped/Converged. **PATTERN**: when detecting ACEs on OUs, ALWAYS filter `IsInherited=$false` to avoid matching domain-inherited defaults.
- 2026-07-14T12:27:00+08:00 — BUG 4 FIX: **Stale FD plan with empty principals** — WinLaps FD plan was precomputed in the PLANNING phase (before Phase 2 creates groups). Group resolution returned empty → plan baked allowedPrincipals=[]. Execution block reused that stale plan. Fix: ALWAYS regenerate WinLapsFdPlan fresh at EXECUTION time (after groups exist). **PATTERN**: FD plans that bake live-resolved principals (like NETBIOS\sam) MUST be regenerated at execution time after dependency phases create the objects; planning-phase plans are only good for display/counts. MSA/gMSA/dMSA are IMMUNE — they store raw config strings in the plan and resolve via NTAccount.Translate() at apply time (late resolution).
- 2026-07-14T12:05:00+08:00 — BUG 3 FIX: **Group lookup by display Name vs sAMAccountName**. Config stores group DISPLAY names with spaces (e.g. "Tier 0 Server Operators", "Tier 2 Help-desk Operators"). `Get-ADGroup -Identity` matches sAMAccountName/DN/GUID/SID — the AD sAMs are CamelCase WITHOUT spaces (Tier0ServerOperators, Tier2HelpdeskOperators). So `-Identity "Tier 0 Server Operators"` FAILS. **PATTERN**: config display-name → AD group lookups MUST use `-Filter "Name -eq '<display name>'"` (matches the group's Name attribute), NOT `-Identity`. Escape single quotes defensively: `$group -replace "'", "''"`. The Fd planner had this right from the start; the prereq Gate 5 did not. Also confirmed from Wolverine: `Find-LapsADExtendedRights.ExtendedRightHolders` = array of strings in "NETBIOS\sAMAccountName" format (e.g. "TIERLAB\Tier0Admins"). Idempotency detection must compare resolved "NETBIOS\sam" (case-insensitive) against this array. Multi-principal EUD: Converged only when ALL principals present.
- 2026-07-14T11:37:00+08:00 — BUG FIXES from Wolverine lab test: (1) **$schemaDN StrictMode crash** — Variable was only defined inside `if ($IncludeMsa -or $IncludeGmsa -or $IncludeDmsa)` block; WinLaps-only runs hit undefined variable under Set-StrictMode -Version Latest. Fix: added `-or $IncludeWinLaps` to the shared resolution condition so $schemaDN/$schemaVersion/$dfl/$ffl are resolved for any -Include* path. **PATTERN TO REMEMBER**: under StrictMode, conditional-only variable initialization is a recurring PowerShell trap — shared variables used by multiple -Include* gates must be resolved BEFORE any gate that references them, not inside one conditional branch. (2) **Write-IncludeAclPlanActions crash** — Function accessed `$_.Data.identityreference` unconditionally, which exists on MSA/gMSA/dMSA actions but NOT on WinLaps actions (which use `lapsOperation`/`allowedPrincipals`/`ouDn`). Fix: branch on `$_.ResourceType -eq 'LapsPermission'` to render WinLaps actions with their own fields while leaving MSA/gMSA/dMSA path untouched. **PATTERN**: when adding a new ResourceType to a shared display function, always branch on ResourceType or test for property existence — never assume all action types have the same Data shape.
- 2026-07-14T10:57:00+08:00 — CORRECTNESS PASS: LAPS cmdlet signatures confirmed via optional/Deploy-WindowsLaps.ps1 reference. Key findings: (1) `-DomainController $PreferredDC` MUST be on every Set-LapsAD* call (Self, Read, Reset). (2) `-AllowedPrincipals` format = "NetBIOS\sAMAccountName" (e.g. "CONTOSO\Tier0ServerOperators", "CONTOSO\Domain Admins"). (3) -AllowedPrincipals ACCEPTS ARRAYS for multi-principal OUs: @("DOM\g1","DOM\g2"). (4) Group mapping corrected: Tier0/1 Member Servers → Server Operators (not Admins); Tier 2 EUD → TWO principals (Device Operators + Help-desk Operators). (5) Config readGroup/resetGroup now accepts string OR array-of-strings. Normalized to @() internally.
- 2026-07-14T10:30:00+08:00 — Windows LAPS implementation T001–T012 complete. Key differences from gMSA pattern: (1) New-TierModelWinLapsAcl calls Set-LapsAD*Permission cmdlets directly (NOT raw ActiveDirectoryAccessRule/DirectoryEntry). (2) Config uses `ouDn`/`computerSelfPermission`/`readGroup`/`resetGroup`/`isDomainControllerOu` fields (not `targetOUPath`/`identityreference`/`activedirectoryrights`). (3) Each delegation → 3 actions (Self+Read+Reset) vs gMSA's 2 ACEs per delegation. (4) Idempotency detection uses `Find-LapsADExtendedRights` for Self detection; Read/Reset detection deferred to lab validation (ExtendedRightHolders structure needs live testing). (5) Module version = 1.2.0. (6) Phase 10 in FullDeployment (after dMSA Phase 9). NeedsLabValidation: Find-LapsADExtendedRights output structure for read/reset holder matching.
- 2026-07-14T10:30:00+08:00 — LAPS cmdlet parameter signatures (confirmed via optional/Deploy-WindowsLaps.ps1): `Set-LapsADComputerSelfPermission -Identity <OU_DN> -DomainController <DC>`, `Set-LapsADReadPasswordPermission -Identity <OU_DN> -AllowedPrincipals @("NetBIOS\sam") -DomainController <DC>`, `Set-LapsADResetPasswordPermission -Identity <OU_DN> -AllowedPrincipals @("NetBIOS\sam") -DomainController <DC>`, `Find-LapsADExtendedRights -Identity <OU_DN>`. -AllowedPrincipals = "DOMAIN\sAMAccountName" format, array-capable.
- 2026-07-13T18:21:02+08:00 — Windows LAPS technical foundation review complete for specs/003-win-laps. Key paths: `.research/windowslaps/` (all files consumed), `.squad/decisions/inbox/beast-winlaps-technical-findings.md` (40 KB detailed findings; primary deliverable). Spec-Kit folder `specs/003-win-laps/` kept clean — only `checklists/` present per Spec-Kit discipline; spec.md/plan.md/tasks.md authored in Wave 2. Three new cmdlets proposed: `Get-TierModelWinLapsAcl`, `Get-TierModelWinLapsAclFd`, `New-TierModelWinLapsAcl` — mirror exact gMSA convention; new files under `modules/TierModel/public/`. Config shape: `tiermodel-winlaps.json` with `winLapsDelegations` array; each entry has `targetOUPath`, `tier`, `selfPermission`, `readPasswordGroup`, `resetPasswordGroup`. Config property on merged object: `$config.winLapsDelegations`. Lab verification: `TierLab-DC01` has `WinLapsSchema` checkpoint (created 2026-07-13T18:19:38+08:00, parent `DC-Promoted-Clean`) confirmed via `Get-VMSnapshot` — schema-absent test: restore to `DC-Promoted-Clean`; schema-present test: restore to `WinLapsSchema`.
- 2026-07-13T18:21:02+08:00 — Windows LAPS schema check method: (1) Quick presence check: query schema NC for all 6 `msLAPS-*` attributeSchema objects by lDAPDisplayName via `Get-ADObject -LDAPFilter "(objectClass=attributeSchema)(lDAPDisplayName=<name>)"`. (2) Hardened check: verify OIDs (1.2.840.113556.1.6.44.1.1 through .6), `attributeSecurityGuid = f3531ec6-6330-4f8e-8d39-7a671fbac605` on encrypted attributes, and `computer` class `mayContain` includes all 6. HARD STOP on failure: error code `WINLAPS_SCHEMA_MISSING`, quiet non-prescriptive alert, NO `Update-LapsADSchema` name in output, NO step-by-step remediation (Joel OQ-RT-02 decision). Do NOT check `msLAPS-CurrentPasswordVersion` (Server 2025 forest schema only, not installed by Update-LapsADSchema).
- 2026-07-13T18:21:02+08:00 — LAPS delegation mechanics: 3 operations per target OU — (A) `Set-LapsADComputerSelfPermission -Identity <OU>` (SELF write on computer objects), (B) `Set-LapsADReadPasswordPermission -Identity <OU> -AllowedPrincipals <readGroup>` (password query), (C) `Set-LapsADResetPasswordPermission -Identity <OU> -AllowedPrincipals <resetGroup>` (expiration-time write). Separate groups mandatory by default for B and C (Joel OQ-RT-04). Read permission alone cannot decrypt encrypted passwords — two-layer model: directory read + `ADPasswordEncryptionPrincipal`. DC/DSRM: hard-exclude by default (Joel OQ-RT-01); DC objects detected by primaryGroupID=516, userAccountControl flags, serverReferenceBL. `New-TierModelWinLapsAcl` calls these LAPS cmdlets directly (NOT raw DirectoryEntry/AccessRule construction like gMSA/MSA/dMSA).
- 2026-07-13T18:21:02+08:00 — Integration with Deploy-TierModel.ps1: add `[switch]$IncludeWinLaps` to param + `$includeParameters` array. Phase 10 in FullDeployment (after MSA=7, gMSA=8, dMSA=9). Uses `Add-IncludeAclPhaseToDeploymentPlan` (existing). Action type `CreateAcl` with `ResourceType = 'LapsPermission'` and `Data.lapsOperation` field. 3 `CreateAcl` actions per delegation entry → N tiers × 3 added to TotalActions (satisfies point 8: totals go UP). Two open design decisions for Cyclops/Professor X: (1) `Get-TierModelConfig.ps1` exception to add `tiermodel-winlaps.json` to optional files, (2) `Test-TierModelPrerequisites.ps1` exception to add `-IncludeWinLaps` prereq checks. If exceptions denied: WinLaps planners self-contain all prereq checks.
- 2026-05-30T15:30:00+08:00 — Manual Hyper-V lab validation passed for the MSA/gMSA/dMSA feature: clean full deployment with `-IncludeMsa -IncludeGmsa -IncludeDmsa` applied 667 changes with 0 errors and silent audits reported 2/2 compliant for all three account types; the standalone `-OuOnly` -> `-GroupOnly` -> standalone `-Include*` flow also passed with 31 + 26 + 12 applied and 0 audit drift.
- 2026-05-30T15:30:00+08:00 — The `DC-Promoted-Clean` checkpoint currently restores a newer non-effective KDS root key (`d33d6533-a88c-8938-eac2-3c42a8b1838d`) alongside an older effective key, and prereq validation sorts by latest `EffectiveTime`, so gMSA/dMSA lab runs must remove that newer key after each reset until the checkpoint is refreshed.
- 2026-05-29T18:03:29.139+08:00 — Updated the gMSA support task plan to use optional config loading through `Get-TierModelConfig`, classSchema-aware dMSA GUID resolution, read-only DC-invoked KDS validation, and the two-ACE delegation model for MSA/gMSA/dMSA ACL work.
- 2026-05-29T10:10:00Z — Tasks documentation consolidated by Scribe merge. All field name corrections applied and new task items integrated.
- 2026-06-03T21:34:33.354+08:00 — `Deploy-TierModel.ps1` now precomputes optional `Get-TierModel*MsaAclFd` plans before the full deployment summary so `-IncludeMsa`, `-IncludeGmsa`, and `-IncludeDmsa` contribute yellow ACL lines plus aggregate counts in planning mode and reuse the same plans during apply.
- 2026-06-03T21:34:33.354+08:00 — Standalone `-Include*` validation in the Hyper-V lab still requires baseline OU and group deployment first (`-OuOnly -ConfirmApply`, then `-GroupOnly -ConfirmApply`) because the standalone planners validate the target service-account OUs and delegation groups before showing the 4-action ACL plan.
- 2026-06-03T21:34:33.354+08:00 — Verified paths for this UX fix: orchestration changes live in `Deploy-TierModel.ps1`, regression coverage lives in `tests/Integration.Deploy.Tests.ps1`, and manual lab validation runs from `C:\TierLab\Deploy` inside `TierLab-DC01` under PowerShell 7.
- 2026-07-13T11:34:25Z — Windows LAPS Wave-1 & Wave-2 consolidated by Scribe orchestration session. All technical findings merged into decisions.md (consolidated 8 inbox files). Orchestration log created: .squad/orchestration-log/2026-07-13T11-34-25-UTC-beast.md. Spec APPROVED by Professor X (9/9 constitution pass, 10/10 requirements pass, all 5 architecture sections verified). Handed off to Cyclops for implementation wave authoring. Key deliverables archived and marked ready for next phase.
- 2025-07-18 — Phase 16 Pester unit tests (T028-T036) completed. 972 unit tests, 0 failures. Three new test files created: Unit.MsaAclOperations.Tests.ps1, Unit.GmsaAclOperations.Tests.ps1, Unit.DmsaAclOperations.Tests.ps1. Critical Pester 5.7.1 behaviors: (1) `-AtLeastTimes` is not a valid parameter for `Should -Invoke`; use `-Times N` without `-Exactly` for "at least N" semantics. (2) `Mock` calls inside `It` blocks in Pester 5.7.1 can bleed to subsequent tests in the same Context — avoid It-scoped mock overrides by using config objects that exercise the BeforeAll conditional mock, and by moving error-path tests that require a specific cmdlet mock (e.g. `Get-Acl { throw }`) into dedicated sub-Contexts with BeforeAll. (3) When cmdlet source returns `@{...}` hashtables for Summary/Validation properties, use `.Keys | Should -Contain` not `.PSObject.Properties.Name | Should -Contain`. (4) Config delegations stored as hashtables mean `$acl.PSObject.Properties['inheritedObjectType']` always returns null — audit cmdlets set `InheritedObjectType = [Guid]::Empty` in expected entries, so mock ACEs must also use `[Guid]::Empty` for InheritedObjectType on the GenericAll/Descendents rule.
