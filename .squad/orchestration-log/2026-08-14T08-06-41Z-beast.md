# Orchestration Log: Beast Lab Spike — Audit SACL Merge Logic

**Date:** 2026-08-14T08:06:41Z  
**Agent:** Beast (Core Dev)  
**Why:** Validate audit-SACL merge logic in lab before writing production code  
**Mode:** Background spike (lab hypervisor: TierLab-DC01)  
**Branch:** feature/domain-auditing  

## Outcome

✅ **All proof points GREEN**. Result: decision document recorded in `.squad/decisions.md`.

### Key Findings Documented

1. **SACL Read API:** `Get-Acl -Path "AD:<dn>" -Audit` / `Set-Acl` (requires `SeSecurityPrivilege` and `AD:` PSDrive from ActiveDirectory module).
2. **AddAuditRule merge behavior:** Merges rights into matching ACE by SID/AuditFlags/InheritanceType; does NOT stack duplicates.
3. **Converge recipe:** Remove-then-add pattern via same `$acl` object; validates no-clobber via `AuditFlags=Success` scope.
4. **Idempotency PASS:** Already-complete state (1 ACE, all 9 rights) performs zero writes on re-run.
5. **No-clobber PASS:** Unrelated ACEs (other SIDs, Failure flags, Inherit=None) untouched.
6. **Baseline SACLs:** 5 default rules on clean domain root; converge replaces 2 duplicate `Everyone/Success/All/WriteProperty` ACEs with 1 canonical ACE.

### 1 Open Question

Joel must confirm: Is it acceptable to replace the two pre-existing default `Everyone/Success/All/WriteProperty` ACEs with our canonical ACE? The canonical is a superset (all 9 rights, not just WriteProperty). **Awaiting decision before production code starts.**

## Impact

Ready to proceed to production implementation. Lab spike validated the merge logic. Decision document includes all 7 design decisions and the converge recipe.

---

**Result:** `.squad/decisions.md` updated with full audit-SACL design decision (with recipe, gotchas, multi-DC notes). Spike script retained in `.research/copilot-cli-hyperv-ad-lab/scripts/audit-spike/` for future reference.
