# Rogue Orchestration Log — Config Validation Wire-In Session

**Session:** 2026-09-04  
**Agent:** Rogue (Core Developer)  
**Branch:** fix/gpo-silent-skip-and-false-success

## Assigned Work

Fixed BUG-023 in modules/TierModel/TierModel.psm1 — schema validation was gated on FromPath parameter set only, causing FromConfig path (used by Deploy and Audit) to skip all schema checks.

## Outcome

**Status:** Verified, shipped in commit c973611.

- **Decision:** Explicit schema load in FromConfig branch using PSScriptRoot-anchored path resolution.
- **Alternative rejected:** Adding SchemaPath to both parameter sets would require test updates and broaden public API surface.
- **Hard gate:** Missing schema now returns Valid=False, not silent pass.
- **Files changed:** modules/TierModel/TierModel.psm1 (2 edits).
- **Test impact:** 1 unit test broken (fixture uses config requiring validation) — delegated to Wolverine for repair.

## Unit Test Results

- Before: 1,573 passed / 0 failed
- After: 1,572 passed / 1 failed (expected)
- Verified: Lab validation pass on TierLab-DC01 under real DC deployment.

## Notes

The fix is minimal and surgical. The entire branch was created to eliminate silent validation skips; permitting missing-schema-as-success in one code path would have reproduced the original defect.