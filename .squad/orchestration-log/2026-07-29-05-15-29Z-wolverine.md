# Orchestration Log: Wolverine BUG-010 Lab Validation

**Timestamp:** 2026-07-29T05:15:29Z  
**Agent:** Wolverine (Tester)  
**Task:** BUG-010 LAB VALIDATION on Hyper-V VM TierLab-DC01 (domain tierlab.internal)  
**Mode:** Background (read-only-to-source; validation only)  
**Outcome:** ✅ PASS — BUG-010 RESOLVED

## Why Wolverine Was Chosen

- Tester role: specialized in lab validation and deployment testing
- BUG-010 scope: requires hands-on VM testing, not code review or analysis
- Background mode: long-running validation suitable for autonomous execution with periodic status reporting
- Files authorized: guest C:\TierModel deploy tree (read/write), transcripts under C:\TierModel\_labruns (read/write)

## Inputs

**Fixed files staged to guest (SHA256 host==guest verified every run):**
- `modules/TierModel/public/New-TierModelOu.ps1` (verify+retry loop)
- `Deploy-TierModel.ps1` (hard-stop gate before Phase 2)

**Lab environment:**
- VM: TierLab-DC01 (Hyper-V, Windows Server 2025)
- Domain: tierlab.internal
- Baseline checkpoint: WinLapsSchema (LAPS schema pre-deployed)
- Clean restores: 4x before TEST A, 1x before TEST B

## Outputs

**Evidence generated:**
- TEST A (4 iterations, -OuOnly): All 10 block-inheritance OUs showed gPOptions=1 every iteration, including previously-failing "Tier 1 Server Staging"
- TEST B (1 run, -FullDeployment): Phase 2 cleared without hard-stop; 10 OUs gPOptions=1; audit 31 OUs / 100% compliance
- Attempts readback verification confirmed: Attempts=1 logged for every OU, proving the post-write comparison path active on every run
- No false-success lines; no Attempts>1 race condition triggered (would require fault injection, out of scope)

**VM state preservation (guardrails verified by coordinator post-run):**
- VM: LEFT RUNNING (per instructions)
- Source hashes: UNCHANGED (no unauthorized edits)
- Git HEAD: UNCHANGED (no commits/pushes)
- Scribe output: Only history.md + decision drop-file written

## Critical Finding

**Operational gotcha discovered during validation:**  
`Audit-TierModel.ps1` must run with working directory = C:\TierModel because it resolves `config/dependencies.json` via a relative path. Running via `pwsh -File` without `Set-Location` fails prerequisites immediately. This is pre-existing (unrelated to BUG-010 fix scope) but relevant for team deployment procedures.

## Result

**All tests passed. BUG-010 fix validated as production-ready.**

The verify+retry loop in `New-TierModelOu.ps1` correctly prevents silent no-ops. The hard-stop gate in `Deploy-TierModel.ps1` correctly prevents false-success propagation to Phase 2. Operational observability (Attempts= log) provides team visibility into retry behavior.

**Recommendation:** Close BUG-010 as RESOLVED. Inform team of Audit-TierModel.ps1 CWD requirement via separate operational note.
