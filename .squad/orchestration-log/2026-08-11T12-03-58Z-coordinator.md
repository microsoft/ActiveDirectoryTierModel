# Orchestration Log - Coordinator

**Date:** 2026-08-11T12:03:58Z
**Session:** canonical-ACL / non-canonical-root-DACL workstream
**Lab:** Hyper-V TierLab-DC01 (tierlab.internal)

## Summary

Empirically reproduced the non-canonical-root bug and built four lab-only diagnostic/repro scripts:
- Show-AclCanonicalOrder.ps1 — read-only; lists DACL ACEs in order, prints .NET IsCanonical
- Invoke-OuAclCanonicalRepro.ps1 — mirrors New-TierModelOuAcl core; prints exception
- Set-LabNonCanonicalAcl.ps1 — injects explicit ALLOW-before-DENY via S.DS.P (lab-destructive)
- Invoke-GateValidation.ps1 — validates gate end-to-end (non-canonical root → deploy hard-stops)

All scripts under .research/copilot-cli-hyperv-ad-lab/scripts/bug006/ (gitignored).

## Lab Findings

- Confirmed: Non-canonical DACL → AddAccessRule throws System.InvalidOperationException
- Detection API: CommonSecurityDescriptor(isContainer=$true,isDS=$true,sdBytes,0).DiscretionaryAcl.IsCanonical
- Engineering note: isDS flag MUST be true for AD or object-ACEs mis-parse
- PS 5.1 quirks: Scripts must be pure ASCII; -band on enums requires [int] cast
- Design validation: Child OUs inherit canonicalized; only root DACL gate needed

## Validation

- Two non-canonical entries injected on domain root
- Deploy hard-stops for -OuOnly, -FullDeployment, -OuAclsOnly
- Exit code 1, zero objects created
- Message names first offender only (TIERLAB\Global_HelpDesk)

## Status

COMPLETE — Lab-validated, pending owner code review. No commit (per owner request).
