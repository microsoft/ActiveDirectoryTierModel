# Orchestration Log - Beast (Core Dev)

**Date:** 2026-08-11T12:03:58Z
**Session:** canonical-ACL / non-canonical-root-DACL workstream
**Branch:** fix/bug-006-canonical-acl-check

## Summary

Implemented the canonical-ACL pre-flight gate in modules/TierModel/public/Test-TierModelCanonicalAcl.ps1 with two parameter sets:
- **ByServer**: Live-DC read via System.DirectoryServices.Protocols (DACL-only with Signing+Sealing)
- **ByBytes**: Unit-testable with mocked canonical/non-canonical SD bytes

Gate wired unconditionally into Test-TierModelPrerequisites after the WinLaps block. Uses CommonSecurityDescriptor(isContainer=$true,isDS=$true).DiscretionaryAcl.IsCanonical for detection with rank-based first-offender scan. SID translated to NTAccount with fallback.

Exported Test-TierModelCanonicalAcl in modules/TierModel/TierModel.psd1 (alphabetical order).

## Design Notes

- Detect-only; never rewrites ACLs
- Both Deploy and Audit share Test-TierModelPrerequisites, so Audit also hard-stops on non-canonical root (intentional per owner)
- Gate placement: unconditional, late in prerequisites, guarded by AD module check
- Exception handling: records RootAclCheckError, no hard-fail on DC connectivity issues

## Validation

- Parse checks: zero errors
- Module import: clean, Test-TierModelCanonicalAcl resolves correctly
- Lab-validated: non-canonical SD → IsCanonical=False, FirstOffender identified; canonical SD → IsCanonical=True

## Status

COMPLETE — Lab-validated, pending owner code review. No commit (per owner request).
